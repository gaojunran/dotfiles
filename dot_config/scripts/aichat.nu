export alias "？" = aichat
export alias ai = aichat
# execute shell command with aichat
export alias aix = aichat -e
# get code only with aichat
export alias aic = aichat -c
export alias trans = aichat "Translate to Chinese if it's English, and Translate to English if it's Chinese. If it's a word, try to give me its meanings as many as possible: "
